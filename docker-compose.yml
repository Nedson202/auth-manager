version: '3.6'

services:
    api:
        container_name: user-service
        restart: always
        build: .
        ports:
            - 80:7000
        volumes:
            - ./:/go/src/github.com/user-service